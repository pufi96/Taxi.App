namespace Taxi.App
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();

            //Routing.RegisterRoute("ride", typeof(RideDetailPage));
            //Routing.RegisterRoute("ride/add", typeof(RideAddEditPage));
            //Routing.RegisterRoute("ride/edit", typeof(RideAddEditPage));
        }
    }
}
